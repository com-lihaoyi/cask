database {
  # Production database uses persistent storage
  path = "/var/lib/cask-todo/data"

  pool {
    max-connections = 25
    min-idle = 5
  }
}

# Disable initial data insertion in production
initial-data {
  enabled = false
}
